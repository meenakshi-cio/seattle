name: Scrape listings

on:
  workflow_dispatch:           # manual only until SMS is verified

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Run scraper
        env:
          CIO_APP_API_KEY: ${{ secrets.CIO_APP_API_KEY }}
          CIO_SITE_ID: ${{ secrets.CIO_SITE_ID }}
          CIO_TRACK_API_KEY: ${{ secrets.CIO_TRACK_API_KEY }}
          CI: "true"
        run: python scraper.py

      - name: Commit updated state
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/listings.json seen_listings.json
          git diff --cached --quiet || git commit -m "listings: routine refresh"
          git push
